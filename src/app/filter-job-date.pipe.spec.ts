import { FilterJobDatePipe } from './filter-job-date.pipe';

describe('FilterJobDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterJobDatePipe();
    expect(pipe).toBeTruthy();
  });
});
